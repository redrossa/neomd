# M1-15 inert content packet and deferred final-testing recipe

[Issue #15](https://github.com/redrossa/neomd/issues/15) · [accepted plan](../../m1-15-content-plan.md) · [seven-criterion map/selectors/evidence](../../m1-15-validation.md).

Status: **ACCEPTED triage readiness; data/oracles only, not executable tests or passed behavior.** Runtime Astra/high; exact source base `f0bd08806013ae8cb4ac994676b30bbe19dc19f5`. D1 offline libyaml and D2 scalar/empty delimiter/recovery approvals are linked in the plan. All native/visual/AX/interaction checks below are **DEFERRED/UNRUN**. Do not perform them now: current worker authority is Debug + relevant NON-INTERACTION units only, no reviewer, UI/E2E, prototypes or manual scripted interactions. #14 skipped/open; #15–24 sequentially authorized. #36 and #38 retain their failures/deferrals, not fixed by this packet. Final combined acceptance is user-owned.

## Inventory and exact data authority

All files are UTF-8. Ordinary physical Markdown files use LF with a final LF; `bom-crlf.md` has exactly one initial UTF-8 BOM and CRLF, `cr-only.md` only CR. JSON `source`/`payload` strings are decoded JSON scalars: `\r`, `\n`, `\t`, `\ufeff` and `\u0000` mean real scalars, not visible escape spellings. Do not normalize line endings/BOM/whitespace. `manifest.json` authenticates every sibling fixture/data file and this README with byte count and SHA-256 (excluding itself to avoid recursion). It is an integrity inventory, not test output.

| File | Purpose and independently intended outcomes |
|---|---|
| `metadata.md` | Valid leading metadata table before `Body start`. Ordered keys, Unicode, textual date/version/yes, contributors/versions nested collections, decoded folded/literal multiline values, plain Markdown-looking DATA, distinct empty states, duplicate authored keys. Delimiters/quotes/indentation are not shown as YAML source in valid table. Body has real alert/task/cues, two footnote references, custom anchor and duplicate heading; metadata reference/footnote/heading text cannot enter those registries. Two adjacent `absent.png` alternatives keep separate occurrence identities. |
| `aliases-tags.md` | Acyclic alias reuse, anchor-name shadowing, inert custom/standard/binary tags and a structured sequence key. All values retain readable context/order; no constructors, fetches or execution. Full bounded table expected. |
| `recovery.md` | Deliberately malformed closed envelope. Entire original candidate, including delimiters and FALLBACK markers, remains plain literal content with quiet explanation. No heading/reference/footnote/link/image/HTML/cue execution from fallback. `Recovery body` and its explicit body link still render normally. This is C5 recovery, not successful C7 table evidence. |
| `comments.md` | True comments with unique HIDDEN sentinels in heading/prose/block suffix/task/quote/link label/image alternative/note; those sentinels absent in owned visible/AX-facing output. ESCAPED, ENTITY, CODE, FENCED, INDENTED, ATTRIBUTE, RAWTEXT, PRE-CODE and UNCLOSED markers remain intentional literal controls. Native supported sub/sup/ins kept; unsupported div/script stays literal, never executable. |
| `bom-crlf.md`, `cr-only.md` | Physical byte fixtures matching named boundary vectors; normal table and body. Assert original physical line mapping and byte preservation. |
| `nearby.md` | Local link/fragment target `nearby-destination`, marker NEARBY DESTINATION. No remote resource needed. |
| `boundary-cases.json` | Exact source/body/root/start-line vectors for approved D2 and all no-envelope controls; includes scalar/empty ambiguity, `...`, EOF, BOM and all line endings. A negative means unchanged ordinary Markdown input, not every delimiter literally displayed. |
| `metadata-cases.json` | Independent ordered semantic oracles for decoded strings/structure/empties/duplicate keys/aliases/tags/NUL and malformed cases. These are not production Swift types or a general YAML constructor schema. |
| `comment-cases.json` | True-token removal and code/escape/entity/attribute/rawtext controls, special inline comment forms, empty task/anchor carriers, exact text/slug/link/alt/style expectations and punctuation. |
| `resource-cases.json` | Declarative repetition/join recipes for parse bytes/depth/nodes/events, output/alias visits, cycles, body boundary and concurrent lifetime units. No executable generator; worker owns tests. Exact bytes/depth/node recipes and isolated event counts are independent of implementation. Projection-specific output counts need worker's explicit row-label contract, not self-comparison. |

`absent.png` is deliberately absent; do not add it or fetch anything for it. Only local explicit body links exist in physical documents. Tag values, scripts and Markdown-looking metadata are inert text; do not execute/evaluate them to inspect these fixtures. Existing broader image fixtures may be combined at final user testing without modifying their historical bytes; no #19 pipe-table scope is added.

## Current non-interaction consumption

Worker installs this packet after outer manifest and replacement-base verification. Tests may read local fixture bytes directly into production parser/data APIs; they do not ask NeoMD to open documents or launch destinations. Preserve unmodified triage fixture files; if an independently authored oracle contradicts actual authoritative parser semantics, report exact case/source evidence and a proposed correction, not a hidden relaxation. All recipe execution is UNRUN at triage. The validation document supplies inspected selectors and worker evidence slots.

Compare physical BOM/CR fixture bytes with the corresponding decoded JSON `source`. For generated resource recipes, preserve the specified ASCII spelling, join/repeat counts and LF; materialize only in bounded test memory. Do not check in oversized derived files. Parse/resource/alias failure must leave original candidate content intact and a normally parsed body; no partial successful table, no truncation and no reinterpretation as Markdown. Resource limits do not prove native responsiveness for enormous fallback strings.

## Future final user-owned setup — DEFERRED/UNRUN

Only after renewed authorization for native testing:

1. Copy the whole packet to a fresh owned temporary folder, retaining names and exact bytes/line endings. Never open or modify repository originals. Record each copied file's SHA-256 and nanosecond mtime before opening; a fixed mtime such as Unix `1700000000` may be assigned to the owned copies before that snapshot. Keep `absent.png` absent. Record exact app commit, build, macOS, display scale and initial appearance/VoiceOver state. Do not change signing, permissions, default app associations or network configuration as an implicit precondition.
2. Use ordinary Finder Open With or File > Open on the tested app and copied `.md` documents, respecting current native opening/window policy. Scalar/empty/malformed controls in JSON can later be materialized as disposable `.md` copies with exact decoded `source` (metadata payload cases use their specified envelope). No future generation changes the approved source or tests now. Keep unrelated apps/readers/clipboard intact.
3. Repeat the criterion scenarios below in system Light and Dark, wide (~900pt) and narrow (~480pt), with available supported reading sizes at final milestone time. Scale implementation/menus belong #22; do not add public controls for this story. Record observations rather than claiming numeric model bounds prove native layout. Restore initial appearance/VoiceOver state after the authorized session.
4. After all reading/navigation/resize/close actions, compare **both** exact bytes/SHA-256 and nanosecond mtime of every copied source/target. Record actual matches/failures. Cleanup only owned copies/readers; never kill other app processes or erase user history. No save prompt, source edit or preference written into documents is expected.

## Future criterion-linked scenarios — every row DEFERRED/UNRUN

| C | Future actions | Observable expected outcome |
|---|---|---|
| 1 | Read `comments.md` visually and through VoiceOver in document order; inspect link/alt/task/footnote labels. | All true HIDDEN markers absent from page/speech/AX-facing labels and anchors; visible suffixes retained. Literal-control markers are intentionally present, not hidden by an AX modifier. Comment-only source has no fabricated prose; empty authored task/custom-anchor semantics remain. |
| 2–3 | Read/select punctuation vector and inline/fenced/indented examples; compare decoded exact strings and code whitespace. | Escapes display literal punctuation, examples remain exact literal code, no accidental heading/link/image/comment removal. Code overflow remains local rather than page-wide. Selection is an observation, not permission to mutate shared clipboard; any Command-C check needs an agreed isolated clipboard window at final acceptance. |
| 4–5 | Open `recovery.md`, unclosed/scalar/empty and over-budget disposable cases. Continue reading body. | No source mode, extra toolbar or requirement to inspect raw source elsewhere. Closed failures retain complete original candidate as literal content plus quiet explanation; its Markdown/HTML examples are non-executing, body remains ordinary rendered Markdown. Unclosed input is unchanged ordinary Markdown. Record responsiveness/failure honestly, especially large fallback; never call fallback a valid-table pass. |
| 6 | Inspect safe styles and unsupported/attribute/rawtext/code cases; read metadata/tag strings without activating body links. | sub/sup/ins remain supported; no script execution, custom app UI, automatic browser/app launch, metadata-driven image load or interactive metadata link. Unsupported HTML and intended code/data remain readable. Do not invoke scripts as a test action. |
| 7 | Read metadata and aliases/tags fixtures, then nested/scalar/empty/multiline vectors. Resize/switch appearance/reading size; navigate table cells with VoiceOver. | Native readable key/value/index hierarchy in source order, nested/duplicate/structured-key data retained, quoted strings decoded, dates/numeric-looking strings not locale-converted, explicit empty states, no YAML delimiters for valid tables. No clipping/overlap/page-wide forced overflow. Metadata accessible context makes keys/values understandable without duplicate speech; plain data is selectable/read-only, not controls. |
| Integration | In `metadata.md`, follow Body destination/Back/custom anchor, both note references/returns and Nearby after explicit activation; inspect body alert/task/cues and adjacent unavailable images. | Correct body heading/duplicate/anchor/note identity independent of metadata; no phantom definitions from YAML. Alert original-line marker still recognized, cues/tasks preserved, two alternatives distinct, source and separate local target unchanged. Metadata text never becomes navigation/cue instructions. |

Record tested head, actual native source/AX roles, complete relevant spoken sequence/context exit, light/dark screenshots and observed wrapping/selection results. Screenshot existence is not a passed visual review. Pure row-label assertions are not VoiceOver proof. Continue final cross-story checks from the cumulative catalog only under renewed authorization; preserve #36/#38 known failures/deferrals and user-owned milestone business acceptance.
