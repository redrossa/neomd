# M1-P7 / #52 inert window-cascade packet

Status: **TRIAGE-AUTHORED; all native/visual/interaction coverage DEFERRED/UNRUN, not passed.** [Approved published proposal](../../milestone-1-window-cascade-pivot.md), [criterion/source mapping and passive-unit allowlist](../../m1-p7-window-cascade-validation.md). Base: `918cfa40f9fdb7323400021b3a06b11f1ff8cf69`. Triage performed no builds, executable tests, prototypes or app interactions. This is a future user-owned recipe, **not authorization to run it now**. No reviewer; later worker gates are Debug build and relevant non-interaction units only. #14 deferred; #15–24 paused even after #52.

## Reproducible data

- `geometry-cases.json` is inert JSON, not a test runner. It contains 12 full-frame placement vectors and six screen-selection/fallback vectors. Coordinates are global AppKit points with bottom-left origin. Inputs distinguish source frame from default destination full-frame size, including synthetic title-bar heights, negative displays, visible-area insets and oversized/smaller-than-minimum screens. The proposed shift is 24 points right/down; exact 24 is an engineering choice, not an additional user product requirement. Invalid/nonfinite geometry cases are described as strings because JSON has no standard infinity/NaN numbers.
- Reuse the **entire existing** [M1-P6 packet](../m1-p6-command-click/README-fixture.md), without editing its historical files: `docs/source.md`, `docs/Target café.MD`, zero-byte `docs/empty.md`, source-folder/parent root sentinels, `docs/private.md`, `docs/notes.txt` and static `docs/img/badge.svg`. Its `manifest.json` authenticates those eight content inputs. The intentionally missing Markdown/image targets remain absent. This avoids duplicate content and retains #50's path, linked-image, selection and fragment controls.
- For future authorized checks, copy that complete packet to a new tester-owned temporary directory. Snapshot each content input's exact bytes/SHA-256, size, permissions and non-nil full-precision modification time after setup. Never modify repository fixtures or an existing user's file. Permission-denial setup affects only an owned copy and must be restored; record genuine OS denial rather than assuming mode bits work for a privileged process. Do not change OS privacy/security permissions.

## Future final-milestone setup — DO NOT RUN NOW

Record app commit, macOS version, display IDs/topology/scaling, each screen's global frame and **visibleFrame**, menu bar/Dock edge and auto-hide state, source/new/other window identity and full frame including title bar, content frame separately, and appearance. Do not confuse logical points with backing pixels or screenshot top-left coordinates with AppKit Y. Do not alter display/appearance/Dock settings under current authorization; future user changes must be restored.

Use source A at its Source checkpoint, with “retained source selection” selected, and a separate already-open destination B at Existing destination checkpoint with its own selection. The M1-P6 recipe supplies exact content and fragment oracles. Keep a third distinguishable reader C when testing focus changes. Choose a source location with enough room for the default new full frame and small down/right offset; if insufficient, use the edge oracle instead of claiming an exact-offset failure. Default new content is 900×720 at this base; measure actual full-frame size on the tested OS, never assume a 22-point title bar.

A window becoming key does not itself erase the source's reading state. Record source/other full frame, title/location, visible checkpoint, selection and independent fragment before/after. The no-empty-window assertion concerns failed opens, not the valid empty-file message.

## Deferred criterion-linked scenario catalog

Every row below is **DEFERRED/UNRUN**, not passed by documentation, numeric vectors, build or model tests.

| Scenario | Future action and oracle | Criteria |
| --- | --- | --- |
| Interior source-relative placement | Command-click A's Checkpoint target section. A new reader D initially appears with its full-frame top-left slightly right/down from A's captured top-left (24 points under the planned policy), **not centered** on the display. D's size is the existing default unless fitting requires shrinking. A and B frames/presentations/checkpoints/selections remain unchanged; only D gets its target fragment. | C1/C3 |
| Unequal source/new sizes | Resize A smaller and larger before activation, with enough room for D. D's top-left uses A.maxY, not A.minY; it does not copy A's size. Its default full size and title-bar height enter the placement calculation. Repeat a path-bearing self-link and an already-open B destination. | C1/C2/C3 |
| Correct originating reader | Activate from A while B/C are separately identifiable. In a future authorized delayed-acquisition environment, switch active reader, move/resize A or close A after capture. Successful D retains the captured origin and original screen identity (subject to availability/fit), not the later active/moved source geometry. Source closure does not redirect into another reader or cancel #50's independent additional request. No delay hook, automation or window prototype is authorized now. If timing cannot be observed reliably, leave this row unverified. | C1/C3 |
| Screen choice and coordinates | With multiple displays, place A on a non-primary display, including one left of or below the primary (negative X/Y) and a nonzero-origin arrangement. D belongs to the source screen, not whichever is key at completion. Check full frame against that screen's current visibleFrame, not a union of screens or origin-zero rectangle. A straddling displays uses its captured NSWindow.screen identity; record the actual identity rather than guessing. | C1/C2 |
| Each edge/corner | Repeat near top, bottom, left, right and all four corners. Expected frame is the desired source-relative origin clamped within the visible rectangle after fitting size. Offset may shrink/disappear when it cannot fit. No part of the full frame crosses usable bounds; title bar and close/minimize/full-screen controls remain visible and usable. Existing readers never move or resize. | C2/C3 |
| Menu bar/Dock | In user-authorized display arrangements, cover side and bottom Dock/menu-bar insets. D is constrained to the current source-screen visibleFrame. Record actual insets and auto-hide state; do not equate screen.frame with usable area or require unstated Stage Manager/full-screen behavior. | C2 |
| Oversized new frame | Use an available display/usable region smaller than the default full frame. Only D shrinks per axis as necessary, remains fully contained, and hosting does not expand it back outside the area. Check title bar/controls remain accessible; source/other windows stay unchanged. Below the old 480×320 minimum, numeric data covers fit calculations; if no physical setup exists, native minimum/hosting behavior remains unverified. A physically impossible area smaller than native controls is not proof of usable controls. | C2/C3 |
| Display removal/current usable area | Only in a future user-authorized observable setup, remove the source display or change its usable area while acquisition is pending. Same surviving ID uses current usable bounds with captured source origin; removal selects nearest current visible rectangle to the captured top-left, ties by display ID, then clamps. No unrelated active reader supplies geometry. With no display available, no physical visibility pass can be claimed. Restore configuration. | C2 |
| Success and failure preservation | Repeat missing/inaccessible target and canceled/terminated request controls from #50. Zero empty additional readers, no source/other frame or reading-state mutation; actionable feedback stays with the still-live captured source presentation, never a later active reader. Valid empty Markdown still opens its established empty-document message in a correctly placed additional reader. Compare bytes/mtime for all copied inputs. | C3 |
| Other routes unchanged | Ordinary local links replace A in its same unmoved/unresized window; pure anchors stay internal. Command-N retains existing picker/default centering behavior, not #52 cascade. File > Open / Command-O, drops, keyboard/context-menu/AX ordinary activation, external links and safe non-Markdown controls retain #50/#43 behavior. No test now launches handlers or invokes controls. | C3 |
| Combined appearance/read-only | At final user acceptance, repeat representative interior/edge/linked-image cases in light/dark and supported window sizes; verify readable controls/source selection and target fragment, no transient empty reader. Compare every input's bytes and full-precision mtime to baseline. Preserve the existing #36/#38 and VoiceOver/cross-story deferrals. | C1/C2/C3 |

Repeated Command-clicks from an unchanged A may use the same source-relative desired origin; #52 does not introduce a persistent cascade sequence or require each new window to avoid all previous windows. Command-click from the new reader uses that reader as the next source. No new tabs, Spaces/full-screen arrangement feature, geometry persistence or preference is authorized.

## Evidence record and limits

Future tester records each row as passed/failed/unverified with exact commit, geometry/topology, source/new/other identities, screenshots/observations, state and file comparisons, defects and restored environment. Screenshot appearance alone does not prove captured-source timing, actual control usability, fragment landing or read-only behavior. Synthetic geometry units establish arithmetic only; request spies establish transport/model state only. All native observations remain deferred until an authorized user test supplies evidence. Do not close milestone 1 or resume paused stories from issue closure.
