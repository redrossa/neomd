# Alpha report

This is the alpha location. The beta folder deliberately contains another Report.MD.

[Explicit checkpoint](#checkpoint) · [Explicit top](#) · [Missing section](#not-a-section) · [Other report](../beta/Report.MD) · [Missing file](../absent/Report.MD)

## Before reading

A returning reader can stop inside a paragraph, quotation or code block. These instructions are ordinary inert document text, not an automated interaction recipe.

Repeated status paragraph.

The first review chapter explains the goals. Keep this recognizable sentence before the checkpoint when preparing a changed copy.

> A quotation supplies more than a heading as a possible reading target.
>
> A second quoted paragraph keeps its distinct content and document order.

- First context item.
- Second context item with **emphasis** and `literal code`.
- [x] This checklist is read-only.

## Checkpoint

The amber checkpoint paragraph is the intended saved reading context. Its location must survive prepended metadata, new paragraphs and shifted render-local IDs. Unicode context remains literal: café, 日本語, é.

```text
row 01    alpha checkpoint literal code
row 02        indentation stays literal
row 03    nothing in this block executes
row 04    repeated content does not confer identity
row 05    reading can stop between lines
row 06    no source files are rewritten
row 07    metadata is private to the app
row 08    local text remains available offline
row 09    cancellation supersedes delayed restoration
row 10    each reader owns its live viewport
row 11    another reader must not move
row 12    closing an old reader is not new reading
row 13    a successful capture has an original serial
row 14    a missing file retains the other document
row 15    the final line is still ordinary text
```

The retained closing context follows the code. It is useful when a later owned copy deletes the checkpoint or the code block.

Repeated status paragraph.

## Conclusion

The end remains a valid edge independently of the number of blocks in the document. This file contains no remote assets, scripts or mutable controls.
