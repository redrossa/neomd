# M1-P6 / #50 inert Command-click packet

Status: **TRIAGE-AUTHORED; all interactions DEFERRED/UNRUN, not passed.** [Approved contract](../../milestone-1-command-click-pivot.md), [source trace/allowed unit gates](../../m1-p6-command-click-validation.md). Triage base: `71cdd6acef777526f16f4020bc54fb273060b9a0`. No app, prototype, test or external handler was run to create this packet. These future oracles are documentation only, not present authorization to interact with the app. #14 is deferred and #15–24 remain paused.

## Reproducible inputs

Copy this entire directory to a new tester-owned temporary folder, preserving relative names and Unicode spelling. Never modify the repository originals or an existing user's documents. The packet is ordinary UTF-8 Markdown/text and one static SVG without scripts, external references, embedded links or event handlers. The only remote resource is an explicit HTTP(S) text link; no remote images load automatically. `manifest.json` lists SHA-256 for the eight content inputs (excluding this README and itself). Verify those bytes before setup.

| Input | Purpose |
| --- | --- |
| `docs/source.md` | Source A with a scroll/selection checkpoint; local/relative/encoded/query/parent/leading-slash/self links; pure anchors/footnote; quoted/task links; whitespace-only linked label; linked image and missing-image fallback; unlinked-image control. |
| `docs/Target café.MD` | Destination B/C with separated existing-reader and new-reader sentinels; space/Unicode/uppercase extension; normal and literal-percent custom anchors. |
| `root-target.md` | Parent-path sentinel for `../root-target.md`. |
| `docs/root-target.md` | Distinct source-folder sentinel for `/root-target.md`. |
| `docs/private.md` | Readable inert permission-control input, not inherently inaccessible. |
| `docs/empty.md` | Valid zero-byte Markdown success, distinct from failed acquisition leaving a blank window. |
| `docs/notes.txt` | Safe non-Markdown default-handler control, inert and not executable. |
| `docs/img/badge.svg` | Static colored illustration for linked and unlinked image branches. |

Intentionally absent: `docs/intentionally-missing.md`, `docs/img/intentionally-missing.svg`. Do not accidentally supply them. Existing repository M1-P4 fixtures and non-interaction `NativeNavigationTests` already describe executable/application/symlink safety; no executable program or app bundle is supplied here.

At future authorized setup, snapshot each disposable content file's exact bytes (or SHA-256), size, non-nil modification time at full filesystem precision, and permissions. After every scenario, compare bytes and modification times for **all** files, not just the visible source. An OS metadata inspection or file copy is not an app interaction pass. Permission/symlink controls must be established before the scenario baseline is taken and restored afterward. Do not compare the disposable copy's timestamp to a Git checkout timestamp.

## Future user-owned scenario catalog — DO NOT RUN NOW

Record app commit, macOS/Xcode version, source path, window identities/titles, initial checkpoints/selections, result, and any defect. If a fixture is too short to distinguish two positions at the chosen viewport, reduce the viewport height within supported limits or record a different visible checkpoint; do not treat mere window count as proof of preserved reading position. A physical new window receiving key focus is expected and is not loss of the source's document/scroll/selection state.

| Scenario | Deferred action and oracle | Criteria |
| --- | --- | --- |
| Additional reader | With A at Source checkpoint and selected “retained source selection”, Command-click Checkpoint target section. Expect one additional physical reader C at Destination section; A keeps the same native window, title/location, presentation, scroll passage and selection. No empty intermediate reader. | C1/C2 |
| Already-open B | First keep a separate B viewer at Existing destination checkpoint with its own selected text. Repeat A's Command-click section/custom-fragment links. Each activation gets a distinct additional reader; B's position/selection/title and A's state stay unchanged. The custom request lands at Literal percent fragment only in its new reader. Close one viewer later: other viewers remain usable. | C1/C2/C3 |
| Ordinary replacement | From A, ordinary-click Local target section. A's same physical window becomes the destination and receives the fragment; no additional reader is created and existing B stays untouched. | C2 |
| Self path vs pure anchor | Command-click a path-bearing `source.md#source-checkpoint`: additional reader of source, its own section. Command-click path without fragment: additional source reader. Ordinary path-bearing self link retains existing same-reader semantics. Pure `#source-checkpoint`, top/missing anchors and generated footnote/return links stay source navigation rather than opening readers. | C1/C2/C4 |
| Path matrix | Repeat ordinary/Command-click for `./`, `../`, space/Unicode/uppercase target, query+fragment and leading slash. Compare distinct parent/source-folder sentinels and exact-once `literal%2520` decoding. For absolute file-link parity, create a separate disposable Markdown document using the current copy's actual `file:` URL plus fragment; no machine-specific path is checked in. It must go through the same safety policy and appropriate reader disposition. | C2/C4 |
| Text/image route | Repeat on heading/prose/quote/task text, linked SVG and missing-image fallback. Linked labels/attachments keep native selection/link styles and pointing-hand regions. Whitespace-only linked label is a routing edge: record whether parser/native range exists and the actual activation result, do not infer it from markup alone. Clicking unlinked image-only content must not navigate. | C1/C4 |
| Missing/inaccessible acquisition | Ordinary and Command-click intentionally missing target: retain A/B, actionable path/access/location feedback, zero spare windows. For permission denial, change only copied `private.md` permissions to deny reading; record whether OS access is actually denied (privileged processes can bypass mode bits). No OS privacy preference/ACL/security workaround is approved. If denial cannot be reproduced, mark unverified. Restore original permissions. | C3 |
| Failure while destination already open | Keep B's prepared content, then remove/deny only a disposable destination copy before A activates its link. Metadata/read failure must not erase or redirect B or create an empty C. Snapshot the setup mutation separately; do not attribute deliberate fixture changes to NeoMD. Injected unit failures cover transaction paths but are not this actual behavior. | C1/C3 |
| Empty-file success | Command-click Valid empty document: additional reader with the established empty-document message, source preserved. A valid empty input is not a failed-acquisition blank-window defect. | C1/C3 |
| Preserved other routes | Command-N picker success/cancel/failure retains #43 policy. File > Open / Command-O and single-file drops replace the receiving/current reader; multi-Markdown batches keep one-file-at-a-time feedback. External HTTP(S), safe `notes.txt` and guarded executable/application targets retain their existing single explicit handler/reveal policy without NeoMD additional readers. No automatic launches from reading. | C4 |
| Native keyboard and menu | Use existing Option-Tab traversal, link cycling, Return/Space, Escape, ordinary AX link press and source context menu Open/Copy during future user testing. These are ordinary activations, not modifier-based new-reader commands. Control-click remains menu behavior, never a new-reader activation. After a Command-click, subsequent keyboard/menu/AX/ordinary click must not inherit stale pointer intent. Selected-link styling stays, #48 enclosing focus suppression remains, ordinary controls keep their own effects. | C4 |
| Races/cleanup | With delayed acquisition only through an authorized future harness, change active window and verify captured source/destination cannot redirect; cancel/supersede ordinary requests and close/terminate while pending. Confirm no stale failure UI in unrelated readers, no extra empty windows and no loss of other viewers. Do not install a new prototype or delay hook under the current authorization. | C1/C3/C4 |
| Combined read-only/appearance | At future user acceptance, repeat representative source/new-reader checks in light/dark and at supported widths; confirm new window focus does not destroy source selection or scroll. Compare bytes/mtime with baseline, record VoiceOver/native semantics and preserve known #36/#38 limitations. | C1/C3/C4 |

No row is passed by triage. Native mouse tracking, independent window ownership/fragment landing, source-state preservation, actual failures/menus/AX/appearance and interaction read-only evidence remain DEFERRED/UNRUN. Build/model tests may provide supporting evidence only. Worker must record actual allowed evidence in the validation document without promoting these recipes to executed tests. Combined milestone acceptance remains the user's decision; do not close the milestone or advance paused stories.
