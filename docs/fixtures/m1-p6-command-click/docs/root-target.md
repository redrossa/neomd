# Current folder destination

LEADING SLASH SENTINEL. The source link `/root-target.md#current-folder-destination` resolves here in the source's own folder, not to the packet's parent file or filesystem root.
