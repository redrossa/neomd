# M1-22 reading-size fixture packet

Status: inert triage data; **all app/native/visual/keyboard/scroll/AX/relaunch checks DEFERRED/UNRUN**. Nothing here authorizes current interaction testing. Refreshed source triage base `cc9c3ddc7653dc35e8e8b03e16c9dc78cf2ceab2`; contract [#22](https://github.com/redrossa/neomd/issues/22), [approved persistence/#38 policy](https://github.com/redrossa/neomd/issues/22#issuecomment-5642959310). [Validation/allowlist](../../m1-22-validation.md).

## Reproducible inputs

- `mixed.md`: UTF-8/LF, all six headings, prose emphasis/inline code/scripts/underline, code indentation and a wide line, nested ordered/bullet/task lists, nested quotations, all five alerts, metadata, aligned formatted and eight-column tables, repeated content with distinct neighbours, top/middle/nested/table/end/footnote sentinels.
- `second.md`: a distinct local document for shared size and independent positions.
- `policy-cases.json`: nine bounded command transitions, preference decode cases, fractions and state invariants. `nonfinite` symbols are inert descriptions of in-memory values to construct in pure units, not JSON numbers or source Markdown syntax. Source fixtures are never executed.
- No network resources, scripts, executable attachments or auto-opening actions. Explicit links are relative or same-document. Do not mutate checked-in fixtures. Unit readers may load bytes and render them in memory; source-immutability units may use owned temporary copies only.

Engineering size set: 1×, 1.5×, 2×; default/reset 1×. No additional product decision is needed. Scale changes typography/metrics, not source. Font weights and structure preserve hierarchy; not every heading level must have a unique point size.

## Future user-owned final milestone recipe — NOT run or authorized now

After renewed final-testing authorization, use disposable copies with SHA-256 and nanosecond-resolution mtime recorded before the session. Record app head, macOS, keyboard layout, theme, width, preference starting value, screenshots and observed reading sentinels. Preserve user's original preference; isolate or restore only that key, never clear unrelated defaults/history.

1. At both Light/Dark and window widths 900/480, open `mixed.md`. Use View commands and Command-plus/minus/zero (including familiar equals alternate); traverse 1→1.5→2→1.5→1, reset from each size, repeat at boundaries. Confirm menu state and absence of added window buttons. Check from prose, code/table overflow, native text focus and the merged #21 find field; commands must not edit text or conflict with find/Copy/Open/window commands. No-reader command state also needs checking.
2. Inspect all heading levels/prose/code/list/task/table hierarchies, wrapping, table headers/rules/alignment, local code/table overflow, metadata and cue sizing. Preserve original source glyphs/code indentation and read-only tasks. Combine with #16 selection and VoiceOver in final validation; no current AX pass is claimed.
3. At MIDDLE CHECKPOINT AMBER, the nested CHARLIE paragraph and JADE table cell, note the content at the viewport reading line and approximate fraction; change size, reset, repeat rapidly. Expect same passage/nearby fraction, not old pixel offset or unrelated top jump. Exercise top/bottom and a pending history reopen; include scrolling, explicit fragment navigation, #21 find reveal, refresh and a closing/replaced reader while a size transition is pending. Latest user intent must not be undone by stale restoration. Keep an idle cross-leaf selection during size changes: its logical endpoints and copied text remain, with native slices re-derived; if a scale-dependent depth-caption endpoint disappears, the selection clears rather than jumps. Queue scale during active dragging/acquisition. With find active, retain query/current match/highlight; scroll away from the current match before changing size to check the old match does not steal the size locator. A new next/previous/query reveal may supersede restoration. These are deferred native oracles, not source-triage observations.
4. Open `second.md` in another reader and change size. Both readers share size but keep independent positions; new/replacement documents use the current preference. Quit normally, relaunch with no test-scale override and reopen. Expect remembered size. Clear Recent/history later: size stays unchanged. Record real relaunch separately from preference-object reload units.
5. Compare bytes and exact mtime of both source copies after commands, navigation, close and relaunch. No source metadata, scale history field or sidecar may be added. Private position history may update legitimate locator data, but size is a separate UserDefaults preference.

Known #38 larger-size/cue/reflow hangs are explicitly deferred for overlapping #22 size-change outcomes, **not passed**, and still block final milestone acceptance until addressed. Keep failed/incomplete evidence and old tests. #36 equivalence remains unproven; do not broaden this waiver to unrelated defects or failed permitted gates. Stop and record a hang/failure rather than calling a retry, skip or missing execution a pass. #14 remains deferred/open. Final business acceptance belongs to the user.
