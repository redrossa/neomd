# Mixed :smile: `#0969DA`

<a id="mixed-start"></a>

MIXED START. Plain **:heart:** text with `rgb(9,105,218)`, *:woman_technologist:* and [a :+1: link](#mixed-target). Emoji/swatches must not create separate focus stops.

Unlinked image path before :smile: `#FFFFFF` ![Green badge](img/badge.png) after :heart: `#000000` and [plain link](#mixed-target).

Linked image native path before :smile: `#FFFFFF` [![Linked badge](img/badge.png)](#mixed-target) after :heart: `hsl(120,100%,50%)`, **bold** and <sup>:smile:</sup>.

## Native heading [![Heading badge](img/badge.png)](#mixed-target) :smile: `#0969DA`

Missing unlinked path before :heart: `#abcdef` ![Missing badge :smile:](img/absent.png) after :+1: `rgb(0,255,0)`.

Missing linked native path before :heart: `#abcdef` [![Missing linked badge :smile:](img/absent.png)](#mixed-target) after :+1: `rgb(0,255,0)`.

![Image-only alt :smile:](img/badge.png)

[![Linked image-only alt :smile:](img/badge.png)](#mixed-target)

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="img/badge.png">
  <img alt="Picture alt :smile:" src="img/badge.png">
</picture>

> [!IMPORTANT]
> ALERT MIXED BODY. :smile: `#0969DA` [![Alert badge](img/badge.png)](#mixed-target) remains in order with its label.
>
> - [ ] Nested pending :heart: `#FFFFFF`
> - [x] Nested done :+1: `#000000`
>
> Adjacent continuation does not repeat a task marker.

7. Ordered item :smile: `#0969DA`
8. [x] Ordered task :heart: `rgb(0,255,0)`

> Ordinary quote :smile: `#abcdef` ![Quote badge](img/badge.png)

Reference[^cue].

[^cue]: Footnote :smile: `hsl(240,100%,50%)` and [![Note badge](img/badge.png)](#mixed-target).

Literal controls: `:smile:` and `rgb(256,0,0)` and \:smile: stay readable.

Mixed spacer 01. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 02. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 03. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 04. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 05. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 06. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 07. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 08. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 09. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 10. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 11. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 12. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 13. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 14. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 15. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 16. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 17. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 18. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 19. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 20. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 21. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 22. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 23. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

Mixed spacer 24. Long text for scrolling to a distant destination; a resize must retain readable ordered content, not trigger a source reparse.

# Mixed target

MIXED TARGET BODY. [Back to mixed start](#mixed-start) returns to the explicit start anchor. Heading slug/duplicate allocation is separately specified in `context-cases.json`.

MIXED END.
