# Priority link-only fixture correction

Status: **ACCEPTED fixture/docs correction; native geometry and interactions UNRUN.** User approved the link-landing diagnosis proposal. Runtime verified before research: `openai-codex/gpt-6-astra`, high, story-triage. This addendum changes only fixture setup for `NeoMDUITests/PrioritySelectionUITests/testLinkClickVersusDragAndCommandClick`, not its routing/selection criteria or 35 pt heading-landing oracle. It does not authorize broad suites or production changes. Original targeted plan and all identity, ownership, clipboard, resource and timeout restrictions remain in force; milestone acceptance stays user-owned.

## Evidence and exact base

Canonical #64/#65/#50 bodies and complete comments, adopted M1-DAG-001, original priority E2E plan, link diagnosis, full current `PrioritySelectionUITests.swift`, original fixture/oracles and relevant shared helper bodies were researched. Worker checkout HEAD was `dc9056c5a3b47f802bfb9bcc30688e14009753a1`; checkout was dirty and preserved. Main HEAD was `445ed2b897934d339eb01b750d37b03edf392dce`. The external packet manifest distinguishes committed test bases from the actual dirty source read; neither is a claim of tested integration. No newly required story dependency or product decision was found. Harness/redraw repair remains the other worker's responsibility.

The original plan deliberately sizes `selection.md` to fit 900 x 900 for cross-block drags. The link method calls the same `prepare()`, drags link offset 2 to Omega offset 2, clicks the fragment, then demands the heading within 35 pt of the reader top. Reported failed-run geometry has the entire short document visible, so successful clamped routing need not satisfy that oracle. This packet preserves the oracle by adding trailing extent, not by accepting no movement or changing production clamping. Native sufficient extent is a prerequisite to establish at runtime, NOT proven by source paragraph count.

## Minimal worker integration

1. Verify packet/source hashes. Install only these two new files at their same `repo/`-relative destinations:
   - `docs/fixtures/m1-priority-e2e/selection-links.md`
   - `docs/m1-priority-link-fixture-repair.md`
   Both destination bases are absent. If either already exists, reconcile instead of overwriting. Do not replace a shared catalog, original plan, original fixture or oracle JSON. Manifest/handoff stay external.
2. In the worker-owned `PrioritySelectionUITests.prepare`, introduce an explicit default-false link-fixture option (for example `linkFixture: Bool = false`). Only `testLinkClickVersusDragAndCommandClick` passes true. For that option call `support.fixture("m1-priority-e2e/selection-links.md", name: "selection.md")`; otherwise retain `support.fixture("m1-priority-e2e/selection.md")`. Never change the global fixture helper default or choose by broad class/name matching. The runtime basename **must be `selection.md`**: the original path-bearing link remains exactly `selection.md#across-blocks` and must open the same long runtime file, not a short sibling or a missing file. Copy it once, snapshot bytes/mtime normally, and do not overwrite it during the run.
3. Continue loading unchanged `selection-oracles.json`: its 16 authored fragments describe the preserved prefix and all existing expectations. Preserve metadata, heading text/slug, prose, literal code tab/newlines, table order, both link labels/targets, decomposed accent and emoji, original fragment offsets (links 2/20 and Omega 2), expected literal slices and separators. The 32 unique plain trailing paragraphs are outside every existing selected interval and are not a new whole-document selection oracle. Do not extend the other selection/find/appearance fixtures or fragment list. No extra heading, link, image, script, network resource or `lantern` occurrence is appended.
4. The independently owned harness repair must enforce the geometry prerequisites below, preserve drag suppression/same-window counts, ordinary pure-fragment activation, Command-click additional-reader/source-state checks and original 35 pt assertion. Reacquire glyph points after navigation or bounded setup; never reuse pre-scroll screen coordinates. Keep the final ordinary path click source-bound and the additional reader's fragment source-independent. Do not change production to make this test pass.
5. Reconcile source hashes against the concurrently edited worker checkout before modifying test source; dirty hashes are research fingerprints, not permission to overwrite the other worker. Report actual integrated head/hash and corrections. No app build or unit run is needed for this inert packet alone. Executable harness changes require the separately approved worker gates and leases; no unit selector is newly authorized by this packet.

## Explicit runtime geometry gate — before the measured fragment click

Use the owned PID/window/document and actual `DocumentReaderScrollView` clip, not requested window dimensions, screenshot estimates, source line counts or post-click selection. Record finite actual viewport bounds, scrollable document bounds, permitted vertical offset interval, current offset and heading bounds in consistent coordinates after layout settles. Account for insets/flipped coordinates; normalized scrollbar values alone do not establish point distances. If existing permitted observation cannot establish these facts, report BLOCKED/UNRUN rather than inventing geometry or widening production telemetry.

For a downward-positive normalized content coordinate, let `V` be viewport height, `[L,U]` the observed permitted vertical offset interval, `O` the current offset, and `d = heading.minY - clip.minY` in screen points. Before the ordinary pure-fragment click require:

- `V > 0`, all measured rectangles/values finite, `L <= O <= U`, and **positive travel** `U - L > 0`.
- **Initially displaced heading:** `d > 35` pt, outside the unchanged landing tolerance, not already landed. The heading must be observable; preserve bounded setup and stop if it cannot establish displacement.
- **Sufficient remaining extent:** `U - O >= d + 35` pt. This reserves more than the travel needed to place the heading at the reader top and a 35 pt endpoint margin; mere scrollbar existence is insufficient.
- Original link and Omega drag glyph endpoints are inside the actual clip, finite and independently resolved before input. Record the same geometry after the drag and immediately before the click; drag must not change heading position beyond the existing 2 pt tolerance or add a window. Reacquire endpoints if any setup/layout change invalidated them.

Only then does the unchanged post-click `abs(heading.minY - clip.minY) <= 35` distinguish a landing from a no-op. Record measured before/after movement and same-window identity; visibility alone is not success. If settled geometry later invalidates reachability, stop as fixture/setup failure, not a routing pass. The new Command-click reader uses the SAME long file; record its actual viewport/document extent and retain its independent 35 pt landing assertion. A heading already landed in that new reader is the expected result, not a reason to impose the source's pre-click displacement check retroactively. Missing geometry or excessive actual window height remains BLOCKED, not permission to relax 35 pt or expand scope.

## Final-testing record to complete later

All native pointer, drag, Command-click, scroll-extent/landing, source preservation and bytes/mtime-through-interaction checks are **UNRUN by triage**. No builds, executable tests, app launches, AX interactions, production edits or GitHub mutations were performed. Static packet checks establish only exact-prefix preservation, unchanged literal/link expectations, deterministic inert suffix, source/artifact hashes and new-file destinations. Worker records actual runtime geometry, exact fixture hash, clean integration/build/product provenance, selector result and cleanup under the original targeted plan. This packet is not a new milestone acceptance record and must not overwrite the shared main handoff.
